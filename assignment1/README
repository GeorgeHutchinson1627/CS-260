1. Some what we could represent a marble could the marbles could be represented as an integer and the bag could be an array
2. We could add them by putting them ontop of a stack an array
3. We could remove
